name: test_eclipse_digital
description: Test for Eclipse Digital.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  auto_route: ^3.2.4
  flutter:
    sdk: flutter
  http: ^0.13.4

dev_dependencies:
  auto_route_generator: ^3.2.3
  build_runner: ^2.1.10
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  url_launcher: ^6.1.0

flutter:
  uses-material-design: true
